venusian README
===============

Venusian is a library which allows you to defer decorator actions.
Instead of taking actions when a function (or class) decorator is
executed at import time, you can defer the action until a separate
"scan" phase.


